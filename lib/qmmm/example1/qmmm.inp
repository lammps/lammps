# configuration file for QMMM wrapper

mode     mech         # coupling choices: o(ff), m(echanical), e(lectrostatic)
#mode     elec         # coupling choices: o(ff), m(echanical), e(lectrostatic)
handle   qmmm         # name of sysv shmem handle
steps    500           # number of QM/MM (MD) steps
verbose  0  
restart    water.restart  # checkpoint/restart file to write out at end

# QM system config
qmdir    qm-pw        # directory to run QM system in
qminp    water.in     # input file for QM code
qmout    NULL    # output file for QM code
qmout    water.out    # output file for QM code
qmcmd    mpirun -np 4 pw.x  # command to launch PWscf 
qmarg   -npool 1 -ndiag 4   # arguments to PWscf

# MM master config
madir    mm-master    # directory to run MM master in
mainp    water.in     # input file for MM master
maout    water.out    # output file for MM master
maout    NULL    # output file for MM master
macmd    mpirun -np 1 lmp_openmpi   # command to launch LAMMPS master
maarg    -echo screen  -sf opt   # arguments for LAMMPS master

# MM slave config
sldir    mm-slave          # directory to run MM slave in
slinp    water_single.in   # input file for MM slave
slout    NULL  # output file for MM slave
slout    water_single.out  # output file for MM slave
slcmd    mpirun -np 1 lmp_openmpi   # command to launch LAMMPS slave
slarg    -echo screen -sf opt    # arguments for LAMMPS slave

