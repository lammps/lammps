[build-system]
requires = ["setuptools>=42", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "mytest"
version = "0.0.0"
description = ""
authors = [
    {name = ""}
]
requires-python = ">=3.7"

[tool.setuptools]
packages = {find = {}}